language: python
python:
  - 2.7
  - 3.6
install:
  - pip install nose
  - pip install -r requirements.php
script:
  - nosetests tests
notifications:
  email:
    - michael@elsdoerfer.com
branches:
  only:
    - master
